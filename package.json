{
  "name": "daji-docs-animate",
  "version": "1.0.0",
  "description": "Vue3 + SCSS 动画示例文档库",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:gh-pages": "cross-env NODE_ENV=production vite build",
    "deploy": "npm run build:gh-pages && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.1.1",
    "sass": "^1.75.0",
    "vite": "^5.2.8"
  }
} 